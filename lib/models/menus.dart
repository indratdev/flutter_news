import 'package:flutter/material.dart';

class Menus {
  Icon icon;
  String title, subtitle;

  Menus({
    required this.icon,
    required this.title,
    required this.subtitle,
  });

  List listMenu = [];

  // listMenu.add(Menus());
}
